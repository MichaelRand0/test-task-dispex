const initialState = {
  
}

export const streetsReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'value':
      
      break;
  
    default:
      return state
  }
}